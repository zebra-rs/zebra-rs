[package]
name = "vtysh-grpc"
version = "0.5.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
prost = "0.11"
tokio = { version="1", features = ["full"] }
tokio-stream = "0.1"
tonic = "0.9"

[build-dependencies]
tonic-build = "0.9"
