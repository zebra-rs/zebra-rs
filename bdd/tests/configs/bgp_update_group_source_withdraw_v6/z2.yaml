# z2 — route-reflector client (AS 65001), the lower-index client; listener.
router:
  bgp:
    global:
      as: 65001
      router-id: 10.81.0.2
    neighbor:
    - remote-address: 2001:db8:80::1
      afi-safi:
      - name: ipv6
        enabled: true
      enabled: true
      remote-as: 65001
