# z3 — the other route-reflector client (receiver only).
router:
  bgp:
    global:
      as: 65001
      router-id: 10.50.0.3
    neighbor:
    - remote-address: 2001:db8:50::1
      afi-safi:
      - name: ipv6
        enabled: true
      enabled: true
      remote-as: 65001
