# z4 — same session, the prefix withdrawn.
router:
  bgp:
    global:
      as: 65002
      router-id: 10.80.0.4
    neighbor:
    - remote-address: 192.168.80.1
      afi-safi:
      - name: ipv4
        enabled: true
      enabled: true
      remote-as: 65001
