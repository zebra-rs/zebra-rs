# z2 — route-reflector client (AS 65001), the lower-index client; listener.
router:
  bgp:
    global:
      as: 65001
      router-id: 10.80.0.2
    neighbor:
    - remote-address: 192.168.80.1
      afi-safi:
      - name: ipv4
        enabled: true
      enabled: true
      remote-as: 65001
