# z3 — plain iBGP neighbor of z1 (no next-hop knob); listener.
router:
  bgp:
    global:
      as: 65001
      router-id: 10.60.0.3
    neighbor:
    - remote-address: 192.168.60.1
      afi-safi:
      - name: ipv4
        enabled: true
      enabled: true
      remote-as: 65001
