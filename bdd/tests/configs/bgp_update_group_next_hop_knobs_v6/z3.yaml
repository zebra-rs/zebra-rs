# z3 — iBGP neighbor toward which z1 has `next-hop-self`; listener.
router:
  bgp:
    global:
      as: 65001
      router-id: 10.61.0.3
    neighbor:
    - remote-address: 2001:db8:60::1
      afi-safi:
      - name: ipv6
        enabled: true
      enabled: true
      remote-as: 65001
